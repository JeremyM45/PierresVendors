@{
  Layout = "_Layout";
}
@using PierresVendors.Models
<h2>Vendors</h2>

@if (@Model.Count == 0)
{
  <h5>No vendors have been added</h5>
} 
@foreach (Vendor vendor in Model)
{
  <h5><a href="/vendor/@vendor.Name">@vendor.Name</a></h5>
}