@{
  Layout = "_Layout";
}
@using PierresVendors.Models
<h2>Vendors</h2>

@if (@Model.Count == 0)
{
  <h3>No vendors have been added</h3>
} 
@foreach (Vendor vendor in Model)
{
  <h3>@vendor.Name</h3>
}